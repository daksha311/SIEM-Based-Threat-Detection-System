# SIEM-Based-Threat-Detection-System
This project detects: - Brute force login attempts - Suspicious user behavior (e.g., multiple tab switching) - High-frequency request patterns (possible DDoS attacks). Built using Python and Flask, the system processes logs and generates real-time alerts with severity levels. It also includes a simple dashboard for monitoring security events.
